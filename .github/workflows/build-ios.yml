name: Build iOS IPA

on:
  push:
    branches: [ main, master, develop ]
  workflow_dispatch:

jobs:
  run-tests:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Setup Java
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'
    
    - name: Setup Gradle
      uses: gradle/gradle-build-action@v2
    
    - name: Run Unit Tests
      run: gradle :shared:test --no-daemon --info
    
    - name: Upload Test Reports
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: test-reports
        path: shared/build/reports/tests/
        if-no-files-found: warn

  build-android:
    runs-on: ubuntu-latest
    needs: run-tests
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Setup Java
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'
    
    - name: Setup Gradle
      uses: gradle/gradle-build-action@v2
    
    - name: Build Android Debug APK
      run: gradle :androidApp:assembleDebug --no-daemon --info
    
    - name: Build Android Release APK
      run: gradle :androidApp:assembleRelease --no-daemon --info
    
    - name: Upload APK Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: CheckTracker-Android
        path: androidApp/build/outputs/apk/**/*.apk
        if-no-files-found: warn
    
    - name: Create Release
      uses: softprops/action-gh-release@v1
      if: startsWith(github.ref, 'refs/tags/')
      with:
        files: |
          androidApp/build/outputs/apk/debug/*.apk
          androidApp/build/outputs/apk/release/*.apk
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build-ios:
    runs-on: macos-latest
    needs: run-tests
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Setup Java
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'
    
    - name: Setup Gradle
      uses: gradle/gradle-build-action@v2
    
    - name: Build Kotlin Multiplatform for iOS
      run: gradle :shared:iosX64Binaries --no-daemon --info
    
    - name: Upload iOS Framework
      uses: actions/upload-artifact@v4
      with:
        name: CheckTracker-iOS
        path: shared/build/**/*.framework
        if-no-files-found: warn
